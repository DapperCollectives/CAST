const globalStyles = {
  global: (props) => ({
    body: {
      fontFamily: 'body',
      lineHeight: 'base',
    },
    input: {
      fontSize: 'sm',
      lineHeight: 'shorter',
    },
  }),
};

export default globalStyles;
